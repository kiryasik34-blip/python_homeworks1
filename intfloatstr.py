# ЗАДАЧА 2 (обновленный формат, простой уровень)
# Цель: понять, что декоратор срабатывает ПРИ ВЫЗОВЕ функции.
#
# 1) Напиши декоратор trace_result(func).
# 2) Внутри wrapper:
#    - выведи: start <имя_функции>
#    - вызови исходную функцию и сохрани результат в result
#    - выведи: end <имя_функции>: <result>
#    - верни result
# 3) Примени декоратор к функции mul(a, b), которая возвращает a * b
# 4) Вызови: print(mul(4, 5))
#
# Ожидаемая идея вывода:
# start mul
# end mul: 20
# 20
#
# Важно:
# - используй *args, **kwargs в wrapper
# - start/end должны печататься внутри wrapper, не снаружи

